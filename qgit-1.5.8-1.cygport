inherit qt3

DESCRIPTION="Qt git frontend"
HOMEPAGE="http://digilander.libero.it/mcostalba/"
SRC_URI="mirror://sourceforge/${PN}/${P}.tar.bz2"

DIFF_EXCLUDES="help.h"

src_compile() {
	cd ${S}
	cygautoreconf

	cd ${B}
	cygconf
	cygmake QT_UIC="${QTDIR}/bin/uic -nounload"
}
